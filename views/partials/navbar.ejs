<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #ffc107">
    <!-- Container wrapper -->
    <div class="container-fluid" style="background-color: #ffc107">
      <!-- Toggle button -->
      

      <!-- Collapsible wrapper -->
      
        <!-- Navbar brand -->
      <a href="/">
        <img src="./img/logo.png" height="auto" width="100" alt="MadeiraFreshLogo" loading="lazy">
      </a>
        <!-- Left links -->
        
        <!-- Right elements -->
        <div class="d-flex align-items-center">
          <!-- Left links -->
          <ul class="navbar-nav me-auto mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/estacoes">Live data</a>
            </li>
            <% if(authenticated && (userType === "mod" || userType === "admin") ) {%>
            <li class="nav-item">
              <a class="nav-link" href="/validarpublicacoes">Validar Publicações</a>
            </li>
            <% } %>
          </ul>
          <!-- Avatar -->
          <% if(authenticated){ %>
            <form action="/logout" method="post">
              <input class="btn btn-secondary" type="submit" value="Logout"/>
            </form>
          <% } else { %>
          <div class="dropdown show">
            <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
              Login
            </button>
            <div class="dropdown-menu dropdown-menu-right <%= authError  ? 'show' : '' %> <%= authErrorType === 'register' ? 'active' : '' %>" aria-labelledby="dropdownMenuButton" id="authentication-dropdown">
                <section id="authentication" class="dropdown-item container-fluid <%= authErrorType === 'register' ? 'active' : '' %>">
                <div id="toggle-forms" class="row justify-content-center">
                  <div class="col-4 d-flex justify-content-center">
                    <button class="<%= authErrorType !== 'register' ? 'active' : '' %>" id="login">
                      Login
                    </button>
                  </div>
                  <div class="col-4 d-flex justify-content-center">
                    <button id="register" class ="<%= authErrorType === 'register' ? 'active' : '' %>">
                      Register
                    </button>
                  </div>
                </div>
                <%- include('./login') %>
                <%- include('./register') %>
              </section>
            </div>
          </div>
          <% } %>
      </div>
    </div>
  </nav>